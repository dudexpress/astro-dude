---
import BaseBox from "./BaseBox.astro";

interface Props {
  value: string;
  small?: boolean;
}

const { value, small } = Astro.props;
---

<BaseBox title="Stand" small={small}>
  {value}
</BaseBox>