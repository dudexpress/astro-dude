---
import {buildPosts, getPostFiles, slug} from "../../utils.ts";
import NavLinks from "../../layouts/NavLinks.astro";

const allFiles = await buildPosts(getPostFiles());
const publishers = [...new Set(allFiles.flatMap(x => x.frontmatter.designer).filter(Boolean))].sort();
---

<NavLinks title="Autori" items={publishers.map(name => ({name, url: `/designers/${slug(name)}`}))}/>
